# Переменные для шаблонов в routes.json

## Доступные переменные

### Основные поля задачи

- `{issue.key}` - Ключ задачи (например, FLEX-1837)
- `{issue.fields.summary}` - Название задачи
- `{issue.fields.status.name}` - Статус задачи (To Do, In Progress, и т.д.)
- `{issue.fields.issuetype.name}` - Тип задачи (Bug, Task, Story, и т.д.)
- `{issue.fields.project.key}` - Ключ проекта (FLEX, OPS, и т.д.)
- `{issue.fields.project.name}` - Название проекта

### Пользователь

- `{user.displayName}` - Имя пользователя, создавшего/изменившего задачу

### Специальные функции

- `{labels}` - Все теги задачи, объединенные через запятую (например: "tag1, tag2, tag3")
  - Если тегов нет, выводит "нет тегов"
  - Берется из `issue.fields.labels` (массив строк)

- `{issue.url}` — Ссылка на задачу в Jira
  - Формат: `{JIRA_BASE_URL}/browse/{issue.key}`
  - `JIRA_BASE_URL` берется из `.env` (пример: `https://your-company.atlassian.net`)

## Примеры шаблонов

### Простой шаблон
```
*{issue.key}*: {issue.fields.summary}
Статус: {issue.fields.status.name}
```

### С тегами и ссылкой
```
🎉 Создана задача с типом {issue.fields.issuetype.name}

Теги: {labels}
Название: {issue.fields.summary}
📎Link: {issue.url}
```

### С автором
```
✅ *{issue.key}*: {issue.fields.summary}

Автор: {user.displayName}
Статус: {issue.fields.status.name}
Тип: {issue.fields.issuetype.name}
```

## Откуда берутся данные

Все данные приходят из Jira webhook payload. Структура:

```json
{
  "issue": {
    "key": "FLEX-1837",
    "fields": {
      "summary": "Название задачи",
      "status": { "name": "To Do" },
      "issuetype": { "name": "Bug" },
      "project": { "key": "FLEX", "name": "Flex Project" },
      "labels": ["tag1", "tag2", "tag3"]
    }
  },
  "user": {
    "displayName": "Имя Пользователя"
  }
}
```

## Важно

- **Теги** (`labels`) - это массив строк в Jira. Если в задаче нет тегов, массив будет пустым `[]`
- **Тип задачи** (`issuetype.name`) - динамический, берется из Jira (Bug, Task, Story, Epic, и т.д.)
- **Ссылка** (`issue.url`) - формируется автоматически на основе `JIRA_BASE_URL` из `.env`

## Примечания

- Все переменные чувствительны к регистру
- Если поле отсутствует в payload, переменная будет пустой строкой
- Для тестирования используй реальные webhook от Jira или интеграционные тесты
