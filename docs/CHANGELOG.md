# Changelog

## [Production Ready] - 2026-01-20

### ✅ Исправления

- **Исправлен формат запроса к Pachka API**
  - Изменен формат body с `{entity_type, entity_id, text}` на `{message: {entity_id, content}}`
  - `entity_id` теперь отправляется как число, не строка
  - Поле `text` заменено на `content` внутри объекта `message`

- **Добавлена поддержка тегов в шаблонах**
  - Добавлена функция `{labels}` для объединения всех тегов через запятую
  - Если тегов нет, выводит "нет тегов"

- **Добавлена поддержка ссылок на задачи**
  - Добавлена функция `{issue.url}` для формирования ссылок на задачи в Jira
  - Использует `JIRA_BASE_URL` из `.env`

- **Динамический тип задачи в шаблонах**
  - Тип задачи теперь берется из Jira через `{issue.fields.issuetype.name}`
  - Поддерживает все типы: Bug, Task, Story, Epic и т.д.

### 🔧 Улучшения

- **IP блокировки сделаны опциональными**
  - IP блокировки отключены по умолчанию (для локального тестирования)
  - Можно включить через переменные окружения для production
  - `JIRA_ALLOWED_IPS` и `INTERNAL_ALLOWED_IPS` опциональны

- **Улучшена обработка ошибок**
  - Детальное логирование запросов к Pachka API
  - Правильная обработка retryable/non-retryable ошибок

### 📝 Документация

- Добавлен `docs/DEPLOY.md` — инструкция по деплою
- Добавлен `docs/TEMPLATE_VARIABLES.md` — список всех переменных для шаблонов
- Обновлен `env.example` с новыми переменными
- Документация организована в папку `docs/` для лучшей структуры проекта

### 🗑️ Очистка

- Удалены временные отладочные документы
- Удалены скрипты локальной разработки
- Обновлен `.gitignore` и добавлен `.dockerignore`

### 🔒 Безопасность

- `.env` файл в `.gitignore`
- IP блокировки опциональны (можно включить для production)
- Поддержка HMAC подписи для webhook (опционально)
